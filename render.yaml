services:
  - type: web
    name: admin-panel-bitcoin-seo
    env: static
    buildCommand: cd AdminPanel && npm install && npm run build
    staticPublishPath: AdminPanel/build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
